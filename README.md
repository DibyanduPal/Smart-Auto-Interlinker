# Smart-Auto-Interlinker
Create per-post keyword → URL mappings and automatically interlink occurrences site-wide. This plugin stores mappings as post meta (on the post/page that defines the mapping) and uses a cached index to insert links dynamically when posts are viewed.
